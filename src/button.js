//take a str, the button label and return element
/**
 *
 * @param {string} buttonName
 * @returns {Element}
 */
const makeButton = buttonName => `Button: ${buttonName}`;

module.exports = makeButton;
